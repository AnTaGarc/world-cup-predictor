# Precomputed penalty contexts

Run `scripts/precompute_penalty_contexts.py` after both teams in a resolved
Round-of-32 fixture have completed their three group-stage matches. Reviewed
JSON files in this directory are static deployment assets used by both the
desktop and online Streamlit applications.

The application never generates these artifacts during a web request.
