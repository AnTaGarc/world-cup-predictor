# Operator Feedback

- Preserve all accumulated user work in the dirty branch. Do not reset, commit, merge, push, or delete files outside the exact task.
- Audit objective-first. Passing tests support completion but do not replace criterion-by-criterion review.
- The 28 deep dossiers contain complete team statistics but generally no final goal scores and no named player statistics. Treat these absences precisely.
- Current SQLite audit before the loop: 28 score-pending fixtures; 27 of those have imported deep/team statistics; Spain vs Japan is the one pending fixture without deep statistics. Overall storage contains 28 deep xG matches, 28 deep volume matches, and 3,192 deep observations; one deep-stat match already has a recorded result.
- The post-match wording and editor were corrected to distinguish missing score from imported statistics. Verify this in code/tests/rendering.
- Prefer small, isolated fixes with tests. Do not redesign unrelated surfaces.
- If Browser automation is unavailable, use Streamlit AppTest plus existing screenshot evidence and report the limitation honestly.
- New user requirement received while Ralph is active: replace the static calendar behavior with an automatic daily fixture/result refresh, guarantee all known matches for today plus the next two days are selectable, add newly resolved knockout fixtures when teams become known, and show the most probable exact score from the score matrix.
