streamlit>=1.36
pandas>=2.2
numpy>=1.26
requests>=2.32
altair>=5.3
Pillow>=10.4
scikit-learn>=1.5
joblib>=1.4
# statsbombpy fetches StatsBomb Open Data (free, hosted on GitHub) and is
# only used by scripts/import_statsbomb_history.py to back-fill deep team
# stats from WC22/18, Euro24/20, Copa América 24, AFCON 23. The Streamlit
# app does NOT import it at runtime.
statsbombpy>=1.13
# OCR (rapidocr + onnxruntime) intentionally OMITTED for Streamlit Cloud:
# they require system libs (libgl1, libglib2.0-0) that aren't installable
# on the current Debian image. The OCR is only used for the "upload match
# screenshot" flow, which is desktop-only. Install locally if you need it.
