# Example for Streamlit Cloud secrets. Copy this to the Streamlit Cloud dashboard
# under "Settings → Secrets". Do NOT commit a real secrets.toml.
# All values are optional — the app runs without them.

# OPENAI_API_KEY = "sk-..."
# OPENAI_MODEL = "gpt-5.5"
